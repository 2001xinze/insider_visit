<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dianping.mapper.ShopTypeMapper">
    <select id="queryShopType" resultType="com.dianping.pojo.ShopType">
        select tb_shop_type.name
        from tb_shop_type
        order by sort
    </select>
</mapper>